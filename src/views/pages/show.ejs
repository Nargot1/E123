<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= thread.title %>
    </title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/show.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">
</head>

<body>
    <%- include('../partials/main-header.ejs') %>
        <div class="page-container">
            <div class="thread-full">
                <h1>
                    <%= thread.title %>
                </h1>
                <p>
                    <%= thread.text %>
                </p>
            </div>
            <div class="thread-actions">
                <div>
                    <form method="post" action="/like">
                        <input type="hidden" name="id" value="<%= thread._id %>">
                        <button class="btn-like" type="submit"><%= thread.likes || 0 %>♥</button>
                    </form>

                    <form method="post" action="/delete" onsubmit="return confirm('Delete this thread?')">
                        <input type="hidden" name="id" value="<%= thread._id %>">
                        <button class="btn-delete" type="submit">Delete</button>
                    </form>
                </div>
                <div>
                    <a class="btn-edit" href="/edit?id=<%= thread._id %>">Edit</a>

                    <a class="btn-back" href="/">Back</a>
                </div>
            </div>
        </div>
        <%- include('../partials/footer.ejs') %>
</body>

</html>